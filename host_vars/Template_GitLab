## GitLab configuration
# GitLab URL
git_url: "{{ SERVER_git_url }}"
# Enable Omniauth (for SSO connection)
enable_omniauth: true
# For maintenance only - Backup password
# git_backup_pass: "{{ SERVER_git_backup_pass | default(srv_proxy_pass) }}"
